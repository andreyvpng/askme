version: '3'

services:
    db:
        container_name: postgres
        restart: always
        image: postgres
        ports:
            - "5432:5432"
    web:
        build: .
        volumes:
            - .:/code
        ports:
            - "8000:8000"
        depends_on:
            - db
